name = "toolbox-api"
main = "src/worker.js"
compatibility_date = "2026-02-13"

# 绑定 KV 命名空间（下面的 id 需要你创建后填入）
kv_namespaces = [
  { binding = "TOOLBOX_KV", id = "PUT_YOUR_KV_NAMESPACE_ID_HERE" }
]

# 用 secret 存 admin token（不要写死在仓库）
# wrangler secret put ADMIN_TOKEN
